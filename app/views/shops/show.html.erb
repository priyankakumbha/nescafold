<p id="notice"><%= notice %></p>

<div class="jumbotron">
  <h1> <%= @shop.name %></h1>
  <h3> <%= @shop.description %></h3>
</div>


<div class="imageshow">  <%= cl_image_tag(@shop.image) %></div>

<div id='shoplatlng'><%= @shop.latlng %></div>
<div id='map'></div>

<!-- BODY  -->

<!-- MENU ITENS  -->
<!-- BOOTSTRAP  -->
<div class="row">
  <div class="col-md-9">
    <% @shop.products.all.each do |product| %>
        <div class="col-sm-5 col-md-2">
          <div class="thumbnail">
            <%= cl_image_tag(product.image) %>
              <div class="caption">
                  <input type="hidden" name="id" value="<%= product.id %>" />
                  <h3><%= product.name %></h3>
                  <p><%= product.description %></p>
                  <h4>Price: $<%= product.price %>,00 </h4>
                  <p>Quantity:
                    <select class="form-control">
                    <option>1</option>
                    <option>2</option>
                    <option>3</option>
                    <option>4</option>
                    <option>5</option>
                    </select>
                  </p>
                  <button type="button" class="btn btn-default btn-lg">
                    <span class="glyphicon glyphicon-star-empty" aria-hidden="true"></span> ADD
                  </button>
              </div>
          </div>
        </div>
    <% end %>
  </div>
  <div class="col-md-3">
    <div class="order_div">
      <ul class="list-group">
        <li class="list-group-item">
        <span class="badge">14
          <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
        </span>
          prouct_name
        </li>

        <hr>
        <h4>Price: $</h4>
        <hr>
          <%= link_to 'Back', shops_path %>
      </ul>

      <!-- <table>
        <thead>
          <tr>
            <th>Name</th>
            <th>Price</th>
            <th>quantity</th>
          </tr>
        </thead>
        <tbody class="order_list">
        </tbody>
      </table>
      <p>Total</p> <p class="total"></p>
    </div>
    <button class="order_btn">order</button> -->
  </div>
</div>
